node_modules/
build/

# env / secrets (never commit real values)
.env
.env.*
!.env.example
!.env.sample
!.env.template
.env.local
.env.*.local
.envrc

# Firebase mobile-style config (if added later)
google-services.json
GoogleService-Info.plist

# keys / npm auth
*.pem
*.key
.npmrc
!/.npmrc.example

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.cache/
coverage/
